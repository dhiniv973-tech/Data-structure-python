class Solution:
    def findDuplicates(self, arr):
        count = {}
        res = []
        
        for num in arr:
            count[num] = count.get(num, 0) + 1
        
        for key in count:
            if count[key] == 2:
                res.append(key)
        
        return res
